.wrapper2
  .header
    %a.header__logo
      = image_tag "logo.jpg", size: '100x60'
    %a.header__user-date
      user
    %a.header__record-btn
      .header__record-btn__record
        = link_to "記録する", new_user_action_record_path(user_id: current_user.id)
        = link_to "ログアウト", destroy_user_session_path, method: :delete,class: "header__record-btn__logout"
      
  .main
    .main__left
      .main__left__edit
        = link_to "編集", "main__left__edit__btn"
      .main__left__life-target
        人生目標、幸せ
      .main__left__life-target-input
        多くのことへの挑戦と好奇心を満たす波乱万丈な人生
      .main__left__doream-target
        夢、自己実現
      .main__left__doream-target-input
        作家
      .main__left__Achievement-target
        達成目標
      .main__left__Achievement-target__input
        手に職をもつ、プログラマーになるためにこのアプリを開発
        一日一時間の執筆活動
      
    .main__light
      .main__light__records
        円グラフ
      %h1.main__light__title
        行動平均値
      .main__light__record-pie-chart
        %ul.main__light__record-pie-chart__left
          %li.sleep
            .sleep__title
              睡眠
            .sleep__time
              - record_total  = @record.sum(:sleep) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .sleep__propotion
              ％
          %li.work
            .work__title
              仕事
            .work__time
              - record_total  = @record.sum(:work) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .work__propotion
              ％
          %li.effort
            .effort__title
              努力
            .effort__time
              - record_total  = @record.sum(:effort) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .effort__propotion
              ％
        %ul.main__light__record-pie-chart__right           
          %li.takeBreak
            .takeBreak__title
              息抜き
            .takeBreak__time
              - record_total  = @record.sum(:takeBreak) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .takeBreak__propotion
              ％
          %li.challenge
            .challenge__title
              努力
            .challenge__time
              - record_total  = @record.sum(:takeBreak) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .challenge__propotion
              ％
          %li.chores
            .chores__title
              雑務
            .chores__time
              - record_total  = @record.sum(:chores) / @record.ids.length
              - num = record_total.floor(2) * 100
              = num / 24
            .chores__propotion
              ％
